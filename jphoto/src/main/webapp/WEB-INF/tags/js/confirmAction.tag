<script type="text/javascript">
    function confirmDeletion(confirmationText) {
        return confirm(confirmationText);

        /*var response;
         jConfirm( 'Delete photo ' + photoName + '?', 'text', function( answer ) {
         if ( answer ) {
         response = answer;
         jAlert( 'Ok' );
         }
         } );
         return response;*/
    }
</script>
